# The DriftWatch FastAPI service's base URL (the one that also handles
# GitHub webhooks). Points at local dev by default; set to the deployed
# Render URL for a production dashboard build.
NEXT_PUBLIC_API_URL=http://localhost:8000
